import test from 'node:test'; import assert from 'node:assert/strict'; import {parseArgs,run} from './index.mjs';
test('validates argv and emits recorded list contract', async()=>{assert.throws(()=>parseArgs(['read','../../bad','--dir','/tmp']),/usage/);const sdk={listSessions:async()=>[{sessionId:'a',cwd:'/tmp',lastModified:2},{sessionId:'b',cwd:'/else',lastModified:3}]};assert.deepEqual(await run(sdk,parseArgs(['list','--dir','/tmp'])),[{id:'a',summary:null,git_branch:null,created_at:null,last_modified:2,cwd:'/tmp'}]);});
test('reads only supported SDK calls with system messages',async()=>{let options;const sdk={getSessionInfo:async()=>({cwd:'/tmp'}),getSessionMessages:async(_id,o)=>{options=o;return [{type:'system'}]}};const x=await run(sdk,parseArgs(['read','0aaaaaaa-bbbb-4ccc-8ddd-eeeeeeeeeeee','--dir','/tmp']));assert.equal(x.messages[0].type,'system');assert.equal(options.includeSystemMessages,true);});
